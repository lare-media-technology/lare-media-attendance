import * as React from 'react';
import CameraPhotoCapture from '../utils/camera/CameraPhotoCapture';


const DashboardMain = (): JSX.Element => {
	return (
		<CameraPhotoCapture />
	);
};

export default DashboardMain;
